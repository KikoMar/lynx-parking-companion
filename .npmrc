# Required for Create React App compatibility.
# CRA's webpack config and its babel/eslint plugins expect a flat node_modules
# structure where all packages are hoisted to the root (same as npm/yarn classic).
shamefully-hoist=true
